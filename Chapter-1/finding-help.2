#!/bin/bash -eu

echo; echo == "'docker help [subcommand]' also works"

sleep 2
set -x
docker help
docker help ps

set +x
echo; echo "== The 'docker help' command is often shorter than the man page."
